<!-- POINTER — not the active adapter. -->

# Claude Code adapter — moved

The active Claude Code adapter for this repo is at the **repo root**: `../../CLAUDE.md`. Claude Code auto-loads the root `CLAUDE.md` at session start; this nested location is never auto-loaded.

Do not edit here — edit `../../CLAUDE.md`.

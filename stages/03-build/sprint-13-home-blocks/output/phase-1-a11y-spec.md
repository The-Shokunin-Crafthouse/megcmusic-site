# Phase 1 — screen-reader spec (pointer)

The spec lives at its canonical path, `_config/design-system/a11y-spec.md` (the `sc-a11y-spec` skill's artifact, one copy — learning #35). Sections: **HomeBlocks zone (Sprint 13)** and **Announcement block (Sprint 13 Phase 1)**. Written before the build; checked against the built block in `phase-1-parity/block-states.json` (names, tab order, five states, reduced motion).
